{
  "configurations": [
    {
      "type": "lldb-dap",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/build/zorax",
      "args": [],
      "env": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Meson: Build all targets"
    }
  
  ]
}
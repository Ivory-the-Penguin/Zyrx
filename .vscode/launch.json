{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/build/zyrx",
      "args": [],
      "cwd": "${workspaceRoot}",
      "preLaunchTask": "Build"
    }
  ]
}